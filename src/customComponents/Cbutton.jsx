/* eslint-disable react/prop-types */
import React from "react";
import { colors } from "../constants/colors";

export default function Cbutton({ children }) {
  return <button>{children}</button>;
}
